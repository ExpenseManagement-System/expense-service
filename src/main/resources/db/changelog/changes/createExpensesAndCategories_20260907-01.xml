<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Create categories table -->
    <changeSet id="20260907-01" author="akanksha">
        <createTable tableName="categories">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="name" type="VARCHAR(100)">
                <constraints nullable="false" unique="true" uniqueConstraintName="uk_categories_name"/>
            </column>
        </createTable>
    </changeSet>

    <!-- Create expenses table -->
    <changeSet id="20260907-02" author="akanksha">
        <createTable tableName="expenses">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="category_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="merchant" type="VARCHAR(255)"/>

            <column name="amount" type="DECIMAL(12, 2)">
                <constraints nullable="false"/>
            </column>

            <column name="expense_date" type="DATE">
                <constraints nullable="false"/>
            </column>

            <column name="description" type="VARCHAR(500)"/>

            <column name="payment_method" type="VARCHAR(50)"/>

            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="deleted" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Add Foreign Key Constraint from expenses to categories -->
        <addForeignKeyConstraint
                baseTableName="expenses"
                baseColumnNames="category_id"
                constraintName="fk_expenses_category_id"
                referencedTableName="categories"
                referencedColumnNames="id"
                onDelete="RESTRICT"
                onUpdate="CASCADE"/>

        <!-- Indexes for Query Performance -->
        <createIndex indexName="idx_expenses_user_id" tableName="expenses">
            <column name="user_id"/>
        </createIndex>

        <createIndex indexName="idx_expenses_user_date" tableName="expenses">
            <column name="user_id"/>
            <column name="expense_date"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>